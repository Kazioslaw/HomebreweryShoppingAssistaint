@model IEnumerable<HomebreweryShoppingAssistaint.Models.LastCheck>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShopID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastCheckDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShopID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastCheckDateTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LastCheckID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LastCheckID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LastCheckID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
