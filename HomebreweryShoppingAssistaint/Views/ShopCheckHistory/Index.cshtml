@model IEnumerable<HomebreweryShoppingAssistaint.Models.ShopCheckHistory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShopID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShopID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CheckDateTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ShopCheckHistoryID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ShopCheckHistoryID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ShopCheckHistoryID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
